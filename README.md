# IT Ticketing System
A simple command-line ticketing system built with Python and SQLite.

## Features
- Create, view, update, and delete tickets.
- Persistent storage using SQLite.

## How to Run
1. Clone the repository.
2. Install dependencies: `pip install tabulate`.
3. Run the program: `python3 ticketing_system.py`.

## Example
